---
phase: 02-core-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - client/src/types/api.ts
  - client/src/lib/api.ts
  - client/src/hooks/useMetrics.ts
  - client/src/hooks/useInvoices.ts
autonomous: true
requirements:
  - MET-01
  - MET-02
  - MET-03
  - MET-04
  - FAC-01
  - FAC-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "useMetrics() devuelve { data, isLoading, isError, error, dataUpdatedAt } tipados correctamente"
    - "useInvoices() devuelve { data, isLoading, isError, error, dataUpdatedAt } tipados correctamente"
    - "Los tipos MetricsResponse e InvoicesResponse reflejan exactamente el shape del BFF"
    - "connectedClients es `null` en el tipo — nunca un número"
    - "QueryClientProvider está en main.tsx con staleTime: 0 y refetchOnWindowFocus: false"
    - "Los hooks exponen refetch() para el botón de refresh manual"
  artifacts:
    - path: "client/src/types/api.ts"
      provides: "Tipos TypeScript para respuestas BFF"
      contains: "connectedClients: null"
    - path: "client/src/lib/api.ts"
      provides: "Funciones fetch que llaman a /api/metrics y /api/invoices"
      contains: "fetchMetrics"
    - path: "client/src/hooks/useMetrics.ts"
      provides: "Hook TanStack Query para métricas"
      contains: "useQuery"
    - path: "client/src/hooks/useInvoices.ts"
      provides: "Hook TanStack Query para facturas"
      contains: "useQuery"
  key_links:
    - from: "client/src/hooks/useMetrics.ts"
      to: "client/src/lib/api.ts"
      via: "fetchMetrics import"
      pattern: "fetchMetrics"
    - from: "client/src/main.tsx"
      to: "QueryClientProvider"
      via: "wrapping App"
      pattern: "QueryClientProvider"
---

<objective>
Construir la capa de datos completa: tipos TypeScript que reflejan el shape exacto del BFF, funciones fetch con Axios, y hooks TanStack Query v5 para métricas y facturas. Este plan establece los contratos de datos que todos los componentes consumirán.

Purpose: Sin esta capa, los componentes de UI no tienen forma de obtener datos del BFF de forma tipada. Los tipos deben ser exactos — un mismatch silencioso en `connectedClients` o en campos de facturas causará bugs difíciles de detectar.
Output: src/types/api.ts, src/lib/api.ts, src/hooks/useMetrics.ts, src/hooks/useInvoices.ts, y QueryClientProvider en main.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@server/src/routes/metrics.ts
@server/src/routes/invoices.ts
@server/src/lib/schemas.ts
@client/src/main.tsx
@client/vite.config.ts

<interfaces>
<!-- Shape exacto del BFF — extraído de server/src/routes/metrics.ts y invoices.ts -->

GET /api/metrics respuesta exitosa:
```typescript
{
  success: true,
  data: {
    activeClients: number,       // Clientes con estado === 'Activo'
    connectedClients: null,      // SIEMPRE null — MET-02 no disponible en Wisphub API
    pendingDebt: number,         // Suma de saldo > 0 (pesos colombianos)
    monthlyRevenue: number,      // Suma total_cobrado facturas Pagadas del mes
    fetchedAt: string,           // ISO datetime string
  },
  cached?: boolean,              // Presente si sirve desde cache
}

GET /api/metrics respuesta error:
{
  success: false,
  error: {
    code: 'UPSTREAM_ERROR',
    message: string,
  }
}
```

GET /api/invoices respuesta exitosa:
```typescript
{
  success: true,
  data: {
    invoices: Array<{
      id_factura: number,
      fecha_emision: string,         // ISO date string
      fecha_vencimiento: string,     // ISO date string — usar para calcular días de atraso
      fecha_pago: string | null,
      estado: string,                // 'Pagada' | 'Pendiente' | 'Vencida' | 'Cancelada'
      total: number,
      saldo: number,                 // Monto pendiente (0 si pagada)
      total_cobrado: number,
      cliente: {
        nombre: string,
        email?: string,
        telefono?: string,
      },
    }>,
    totalCount: number,
    fetchedAt: string,
  },
  cached?: boolean,
}

GET /api/invoices respuesta error:
{
  success: false,
  error: {
    code: 'UPSTREAM_ERROR' | 'VALIDATION_ERROR',
    message: string,
    details?: unknown,
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear tipos TypeScript + funciones fetch en api.ts</name>
  <files>
    client/src/types/api.ts,
    client/src/lib/api.ts
  </files>

  <read_first>
    - server/src/routes/metrics.ts — shape exacto de MetricsData interface
    - server/src/routes/invoices.ts — shape exacto de InvoicesData interface
    - server/src/lib/schemas.ts — tipos WisphubInvoice para alinear campos
    - client/vite.config.ts — confirmar que /api/* hace proxy a localhost:3001
  </read_first>

  <action>
Crear `client/src/types/api.ts` con todos los tipos derivados de los shapes del BFF:

```typescript
// Tipos derivados del BFF (server/src/routes/metrics.ts y invoices.ts)
// IMPORTANTE: estos tipos deben mantenerse sincronizados con los interfaces del server

// ── Factura individual ──
export interface BffInvoice {
  id_factura: number
  fecha_emision: string         // ISO date string ("2025-01-15")
  fecha_vencimiento: string     // ISO date string — usado para calcular días de atraso
  fecha_pago: string | null     // null si no pagada
  estado: 'Pagada' | 'Pendiente' | 'Vencida' | 'Cancelada' | string
  total: number                 // total en pesos colombianos
  saldo: number                 // monto pendiente (0 si pagada)
  total_cobrado: number
  cliente: {
    nombre: string
    email?: string
    telefono?: string
  }
}

// ── Respuesta GET /api/metrics ──
export interface MetricsData {
  activeClients: number
  connectedClients: null        // SIEMPRE null — MET-02 no disponible en Wisphub
  pendingDebt: number
  monthlyRevenue: number
  fetchedAt: string
}

export interface MetricsResponse {
  success: true
  data: MetricsData
  cached?: boolean
}

// ── Respuesta GET /api/invoices ──
export interface InvoicesData {
  invoices: BffInvoice[]
  totalCount: number
  fetchedAt: string
}

export interface InvoicesResponse {
  success: true
  data: InvoicesData
  cached?: boolean
}

// ── Error response (ambos endpoints) ──
export interface BffErrorResponse {
  success: false
  error: {
    code: 'UPSTREAM_ERROR' | 'VALIDATION_ERROR' | string
    message: string
    details?: unknown
  }
}

// ── Helper: discriminated union para type-safe error handling ──
export type MetricsApiResponse = MetricsResponse | BffErrorResponse
export type InvoicesApiResponse = InvoicesResponse | BffErrorResponse
```

Crear `client/src/lib/api.ts` con funciones fetch usando Axios:

```typescript
import axios from 'axios'
import type { MetricsData, InvoicesData } from '@/types/api'

// Axios instance — el baseURL es relativo porque Vite proxea /api/* a localhost:3001
const http = axios.create({
  baseURL: '/',
  timeout: 15_000,
  headers: {
    'Content-Type': 'application/json',
  },
})

// ── fetchMetrics ──
// Llama GET /api/metrics y devuelve MetricsData (throws en error)
export async function fetchMetrics(): Promise<MetricsData> {
  const response = await http.get<{ success: boolean; data: MetricsData; error?: unknown }>('/api/metrics')

  if (!response.data.success) {
    const err = response.data as unknown as { error: { message: string } }
    throw new Error(err.error?.message ?? 'Error desconocido al obtener métricas')
  }

  return response.data.data
}

// ── fetchInvoices ──
// Llama GET /api/invoices y devuelve InvoicesData (throws en error)
export async function fetchInvoices(): Promise<InvoicesData> {
  const response = await http.get<{ success: boolean; data: InvoicesData; error?: unknown }>('/api/invoices')

  if (!response.data.success) {
    const err = response.data as unknown as { error: { message: string } }
    throw new Error(err.error?.message ?? 'Error desconocido al obtener facturas')
  }

  return response.data.data
}
```

**NOTA sobre errores Axios:** Axios lanza automáticamente para respuestas 4xx/5xx, así que `response.data.success === false` solo se alcanza si el BFF devuelve 200 con `success: false` (caso raro). Los errores 502 del BFF llegan como `AxiosError` con `error.response.data` — TanStack Query los captura automáticamente.
  </action>

  <verify>
    <automated>
      cd client && npx tsc --noEmit 2>&1 | grep -E "error|warning" | head -20
      grep -n "connectedClients: null" client/src/types/api.ts
      grep -n "fetchMetrics" client/src/lib/api.ts
      grep -n "fetchInvoices" client/src/lib/api.ts
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "connectedClients: null" client/src/types/api.ts` devuelve resultado (tipo literal null, no number)
    - `grep "BffInvoice" client/src/types/api.ts` devuelve la interface
    - `grep "fecha_vencimiento" client/src/types/api.ts` devuelve resultado
    - `grep "fetchMetrics" client/src/lib/api.ts` devuelve la función exportada
    - `grep "fetchInvoices" client/src/lib/api.ts` devuelve la función exportada
    - `cd client && npx tsc --noEmit` sin errores en los archivos nuevos
  </acceptance_criteria>

  <done>client/src/types/api.ts con todos los tipos BFF, client/src/lib/api.ts con fetchMetrics y fetchInvoices. TypeScript sin errores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear hooks useMetrics + useInvoices y configurar QueryClientProvider en main.tsx</name>
  <files>
    client/src/hooks/useMetrics.ts,
    client/src/hooks/useInvoices.ts,
    client/src/main.tsx
  </files>

  <read_first>
    - client/src/lib/api.ts — importar fetchMetrics y fetchInvoices (Task 1 prerequisito)
    - client/src/types/api.ts — importar MetricsData e InvoicesData
    - client/src/main.tsx — agregar QueryClientProvider envolviendo App
    - client/package.json — confirmar @tanstack/react-query@5.96.2 ya instalado
  </read_first>

  <action>
Crear `client/src/hooks/useMetrics.ts`:

```typescript
import { useQuery } from '@tanstack/react-query'
import { fetchMetrics } from '@/lib/api'
import type { MetricsData } from '@/types/api'

export const METRICS_QUERY_KEY = ['metrics'] as const

export function useMetrics() {
  return useQuery<MetricsData, Error>({
    queryKey: METRICS_QUERY_KEY,
    queryFn: fetchMetrics,
    staleTime: 0,               // Siempre re-fetch al montar — data fresca en cada carga
    refetchOnWindowFocus: false, // Solo refresh manual — no polling accidental
    retry: 1,                   // Un reintento en error de red transitorio
  })
}
```

Crear `client/src/hooks/useInvoices.ts`:

```typescript
import { useQuery } from '@tanstack/react-query'
import { fetchInvoices } from '@/lib/api'
import type { InvoicesData } from '@/types/api'

export const INVOICES_QUERY_KEY = ['invoices'] as const

export function useInvoices() {
  return useQuery<InvoicesData, Error>({
    queryKey: INVOICES_QUERY_KEY,
    queryFn: fetchInvoices,
    staleTime: 0,
    refetchOnWindowFocus: false,
    retry: 1,
  })
}
```

Actualizar `client/src/main.tsx` para agregar QueryClientProvider:

```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import './index.css'
import App from './App.tsx'

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 0,
      refetchOnWindowFocus: false,
      retry: 1,
    },
  },
})

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
    </QueryClientProvider>
  </StrictMode>,
)
```

**IMPORTANTE:** Los hooks exportan `refetch` desde el objeto retornado por `useQuery`. El componente RefreshButton (Plan 05) llamará a `queryClient.invalidateQueries()` para refrescar ambas queries simultáneamente — no necesita pasar `refetch` por props.
  </action>

  <verify>
    <automated>
      grep -n "QueryClientProvider" client/src/main.tsx
      grep -n "refetchOnWindowFocus: false" client/src/hooks/useMetrics.ts
      grep -n "refetchOnWindowFocus: false" client/src/hooks/useInvoices.ts
      cd client && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "QueryClientProvider" client/src/main.tsx` devuelve resultado con la envoltura
    - `grep "refetchOnWindowFocus: false" client/src/hooks/useMetrics.ts` devuelve resultado
    - `grep "refetchOnWindowFocus: false" client/src/hooks/useInvoices.ts` devuelve resultado
    - `grep "METRICS_QUERY_KEY" client/src/hooks/useMetrics.ts` devuelve `const METRICS_QUERY_KEY`
    - `grep "INVOICES_QUERY_KEY" client/src/hooks/useInvoices.ts` devuelve `const INVOICES_QUERY_KEY`
    - `grep "retry: 1" client/src/hooks/useMetrics.ts` devuelve resultado
    - `cd client && npx tsc --noEmit` sin errores
  </acceptance_criteria>

  <done>useMetrics.ts y useInvoices.ts creados con configuración correcta de TanStack Query v5. QueryClientProvider envuelve App en main.tsx. TypeScript sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| Browser → Vite proxy (/api/*) | Frontend solo habla con Vite proxy — nunca directamente a Wisphub |
| Axios response → tipos TypeScript | El cast de tipos es manual — si el BFF cambia shape, TypeScript no lo detecta automáticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | api.ts — errores Axios | mitigate | Los mensajes de error del BFF se muestran al admin (único usuario). No hay usuarios externos que puedan ver stack traces. |
| T-02-05 | Tampering | QueryClient staleTime | accept | staleTime: 0 significa que siempre se re-fetcha. El riesgo es demasiados requests — mitigado por el BFF cache de 30s. |
| T-02-06 | Denial of Service | useQuery retry | mitigate | retry: 1 — máximo 2 intentos total. Evita request storms en error de Wisphub. |
</threat_model>

<verification>
Verificación global del plan:

1. `cd client && npx tsc --noEmit` — sin errores TypeScript en todos los archivos nuevos
2. `grep "connectedClients: null" client/src/types/api.ts` — tipo literal null confirmado
3. `grep "QueryClientProvider" client/src/main.tsx` — QueryClient configurado
4. `grep "refetchOnWindowFocus: false" client/src/hooks/useMetrics.ts` — no polling
</verification>

<success_criteria>
- client/src/types/api.ts define BffInvoice, MetricsData, InvoicesData con todos los campos correctos
- connectedClients es `null` en el tipo (nunca `number | null`)
- client/src/lib/api.ts exporta fetchMetrics y fetchInvoices
- client/src/hooks/useMetrics.ts y useInvoices.ts usan TanStack Query v5 con refetchOnWindowFocus: false
- QueryClientProvider envuelve App en main.tsx
- TypeScript compila sin errores
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-core-dashboard/02-02-SUMMARY.md` con el formato estándar de summary.
</output>
