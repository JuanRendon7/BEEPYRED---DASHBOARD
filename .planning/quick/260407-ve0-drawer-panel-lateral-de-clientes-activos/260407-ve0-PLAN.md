---
phase: quick
plan: 260407-ve0
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/clients.ts
  - server/src/index.ts
  - client/src/types/api.ts
  - client/src/lib/api.ts
  - client/src/hooks/useClients.ts
  - client/src/components/metrics/ClientsDrawer.tsx
  - client/src/components/metrics/MetricCard.tsx
  - client/src/components/metrics/MetricsGrid.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Hacer click en la tarjeta 'Clientes activos' abre el drawer mostrando solo clientes con estado Activo"
    - "Hacer click en la tarjeta 'Clientes suspendidos' abre el drawer mostrando solo clientes con estado Suspendido"
    - "Clicking fuera del drawer o en el botón X lo cierra"
    - "El header sticky del dashboard NUNCA queda tapado por el drawer"
    - "El drawer muestra nombre, plan, zona y saldo (formato COP) por cada cliente"
    - "El saldo se muestra con Intl.NumberFormat locale es-CO igual que el resto del dashboard"
    - "El endpoint GET /api/clients responde con datos cacheados 30s igual que /api/metrics"
  artifacts:
    - path: "server/src/routes/clients.ts"
      provides: "BFF route GET /api/clients"
      exports: ["clientsRouter"]
    - path: "client/src/components/metrics/ClientsDrawer.tsx"
      provides: "Panel lateral deslizable con lista de clientes filtrada"
      exports: ["ClientsDrawer"]
    - path: "client/src/hooks/useClients.ts"
      provides: "TanStack Query hook para datos de clientes"
      exports: ["useClients", "CLIENTS_QUERY_KEY"]
  key_links:
    - from: "client/src/components/metrics/MetricsGrid.tsx"
      to: "ClientsDrawer"
      via: "useState drawerEstado + onClick en MetricCard"
    - from: "client/src/hooks/useClients.ts"
      to: "/api/clients"
      via: "fetchClients en queryFn"
    - from: "server/src/routes/clients.ts"
      to: "fetchAllPages + WisphubClientSchema"
      via: "import desde lib existente — misma lógica que metrics.ts"
---

<objective>
Agregar un drawer (panel lateral) interactivo al dashboard BEEPYRED. Al hacer click en "Clientes activos" o "Clientes suspendidos", se desliza un panel desde la derecha mostrando la lista completa de esos clientes filtrada por estado.

Purpose: El administrador puede ver qué clientes específicos conforman cada métrica sin salir del dashboard ni abrir Wisphub directamente.
Output: Endpoint BFF /api/clients + hook useClients + componente ClientsDrawer integrado en MetricsGrid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<!-- Interfaces clave que el executor necesita — no requiere exploración del codebase -->

De server/src/lib/schemas.ts:
```typescript
export const WisphubClientSchema = z.object({
  id_servicio: z.number(),
  nombre: z.string(),
  estado: z.string(),           // "Activo" | "Suspendido" | "Cancelado"
  saldo: z.string(),            // STRING "0.00" — usar parseFloat()
  precio_plan: z.string(),
  plan_internet: z.object({ id: z.number(), nombre: z.string() }).nullable(),
  zona: z.object({ id: z.number(), nombre: z.string() }).nullable(),
  router: z.object({ id: z.number(), nombre: z.string(), falla_general: z.boolean(), falla_general_descripcion: z.string() }).nullable(),
})
export type WisphubClient = z.infer<typeof WisphubClientSchema>
```

De server/src/lib/wisphub.ts:
```typescript
export async function fetchAllPages<T>(
  endpoint: string,
  params: Record<string, string | number> = {}
): Promise<{ results: T[]; count: number }>
```

De server/src/lib/cache.ts:
```typescript
export const cache: { get<T>(key: string): T | null; set<T>(key: string, data: T, ttlMs: number): void }
```

De server/src/index.ts (patrón de registro de routers):
```typescript
import { metricsRouter } from './routes/metrics'
app.use(metricsRouter)
// Agregar: app.use(clientsRouter) ANTES del 404 handler y DESPUÉS de app.use(invoicesRouter)
```

De client/src/lib/api.ts (patrón de fetchXxx existente):
```typescript
const http = axios.create({ baseURL: '/', timeout: 15_000 })
export async function fetchMetrics(): Promise<MetricsData> {
  const response = await http.get<{ success: boolean; data: MetricsData; error?: unknown }>('/api/metrics')
  if (!response.data.success) { throw new Error(...) }
  return response.data.data
}
```

De client/src/hooks/useMetrics.ts (patrón de useQuery):
```typescript
export function useMetrics() {
  return useQuery<MetricsData, Error>({
    queryKey: ['metrics'],
    queryFn: fetchMetrics,
    staleTime: 0,
    refetchOnWindowFocus: false,
    retry: 1,
  })
}
```

De client/src/components/metrics/MetricCard.tsx (interfaz actual):
```typescript
interface MetricCardProps {
  title: string
  value: string
  subtitle?: string
  icon: LucideIcon
  iconClassName?: string
  className?: string
}
// El div raíz usa: className={cn('rounded-xl border border-border bg-surface p-6', 'transition-colors', className)}
```

De client/src/components/layout/DashboardLayout.tsx:
```typescript
// header tiene: className="sticky top-0 z-50 border-b border-border bg-surface px-6 py-4"
// El overlay del drawer debe usar z-40 (debajo del header z-50)
// El panel del drawer debe usar z-40 también
```

De client/tailwind.config.ts (colores del design system):
```typescript
// surface: '#18181B'   — fondo del drawer
// border: '#27272A'    — border-l del drawer
// page: '#0A0A0A'      — fondo overlay semitransparente
// text.primary: '#FFFFFF', text.secondary: '#A1A1AA', text.muted: '#71717A'
// error: '#EF4444'
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF endpoint GET /api/clients + tipos frontend</name>
  <files>
    server/src/routes/clients.ts,
    server/src/index.ts,
    client/src/types/api.ts,
    client/src/lib/api.ts,
    client/src/hooks/useClients.ts
  </files>
  <action>
**1a. Crear server/src/routes/clients.ts**

Seguir exactamente el mismo patrón de server/src/routes/metrics.ts.

```typescript
import { Router, Request, Response } from 'express'
import { fetchAllPages } from '../lib/wisphub'
import { WisphubClientSchema } from '../lib/schemas'
import { cache } from '../lib/cache'
import { z } from 'zod'

export const clientsRouter = Router()

const CACHE_KEY = 'clients'
const CACHE_TTL_MS = 30_000

interface BffClientRecord {
  nombre: string
  estado: string
  saldo: number          // parseFloat del string de Wisphub
  plan: string | null    // plan_internet.nombre o null
  zona: string | null    // zona.nombre o null
}

interface ClientsData {
  clients: BffClientRecord[]
  totalCount: number
  fetchedAt: string
}

clientsRouter.get('/api/clients', async (_req: Request, res: Response) => {
  const cached = cache.get<ClientsData>(CACHE_KEY)
  if (cached) {
    return res.json({ success: true, data: cached, cached: true })
  }

  try {
    const allRaw = await fetchAllPages<unknown>('/api/clientes/')

    const clients: BffClientRecord[] = []
    let validationWarnings = 0

    for (const raw of allRaw.results) {
      const parsed = WisphubClientSchema.safeParse(raw)
      if (parsed.success) {
        clients.push({
          nombre: parsed.data.nombre,
          estado: parsed.data.estado,
          saldo: parseFloat(parsed.data.saldo) || 0,
          plan: parsed.data.plan_internet?.nombre ?? null,
          zona: parsed.data.zona?.nombre ?? null,
        })
      } else {
        validationWarnings++
      }
    }

    if (validationWarnings > 0) {
      console.warn(`[/api/clients] ${validationWarnings} client records failed Zod validation`)
    }

    const data: ClientsData = {
      clients,
      totalCount: clients.length,
      fetchedAt: new Date().toISOString(),
    }

    cache.set(CACHE_KEY, data, CACHE_TTL_MS)
    return res.json({ success: true, data })

  } catch (err) {
    console.error('[/api/clients] Upstream error:', err)
    return res.status(502).json({
      success: false,
      error: {
        code: 'UPSTREAM_ERROR',
        message: err instanceof Error ? err.message : 'Unknown upstream error',
      },
    })
  }
})
```

**1b. Registrar en server/src/index.ts**

Agregar import y uso del router. Insertar DESPUÉS de `app.use(invoicesRouter)` y ANTES del 404 handler:

```typescript
import { clientsRouter } from './routes/clients'
// ...
app.use(clientsRouter)
```

**1c. Agregar tipos a client/src/types/api.ts**

Agregar al final del archivo (sin modificar lo existente):

```typescript
// ── Cliente individual (BFF normalizado) ──
export interface BffClient {
  nombre: string
  estado: string          // "Activo" | "Suspendido" | "Cancelado"
  saldo: number           // ya parseado como número
  plan: string | null     // plan_internet.nombre o null
  zona: string | null     // zona.nombre o null
}

// ── Respuesta GET /api/clients ──
export interface ClientsData {
  clients: BffClient[]
  totalCount: number
  fetchedAt: string
}

export interface ClientsResponse {
  success: true
  data: ClientsData
  cached?: boolean
}

export type ClientsApiResponse = ClientsResponse | BffErrorResponse
```

**1d. Agregar fetchClients a client/src/lib/api.ts**

Agregar al final del archivo (sin modificar lo existente):

```typescript
import type { MetricsData, InvoicesData, ClientsData } from '@/types/api'

// ── fetchClients ──
// Llama GET /api/clients y devuelve ClientsData (throws en error)
export async function fetchClients(): Promise<ClientsData> {
  const response = await http.get<{ success: boolean; data: ClientsData; error?: unknown }>('/api/clients')

  if (!response.data.success) {
    const err = response.data as unknown as { error: { message: string } }
    throw new Error(err.error?.message ?? 'Error desconocido al obtener clientes')
  }

  return response.data.data
}
```

NOTA: El import de `http` ya existe en el archivo — no redeclares el axios instance. Solo agregar el import del tipo ClientsData y la función al final.

**1e. Crear client/src/hooks/useClients.ts**

```typescript
import { useQuery } from '@tanstack/react-query'
import { fetchClients } from '@/lib/api'
import type { ClientsData } from '@/types/api'

export const CLIENTS_QUERY_KEY = ['clients'] as const

export function useClients() {
  return useQuery<ClientsData, Error>({
    queryKey: CLIENTS_QUERY_KEY,
    queryFn: fetchClients,
    staleTime: 0,
    refetchOnWindowFocus: false,
    retry: 1,
  })
}
```
  </action>
  <verify>
    curl http://localhost:3001/api/clients responde con { success: true, data: { clients: [...], totalCount: N, fetchedAt: "..." } }. Cada elemento tiene nombre (string), estado (string), saldo (number), plan (string|null), zona (string|null). TypeScript compila sin errores: cd client && npx tsc --noEmit
  </verify>
  <done>
    GET /api/clients devuelve 200 con lista de todos los clientes normalizados. Tipos BffClient y ClientsData exportados desde client/src/types/api.ts. fetchClients y useClients funcionan. tsc --noEmit pasa sin errores.
  </done>
</task>

<task type="auto">
  <name>Task 2: ClientsDrawer + MetricCard clickeable + MetricsGrid integrado</name>
  <files>
    client/src/components/metrics/ClientsDrawer.tsx,
    client/src/components/metrics/MetricCard.tsx,
    client/src/components/metrics/MetricsGrid.tsx
  </files>
  <action>
**2a. Crear client/src/components/metrics/ClientsDrawer.tsx**

El drawer se desliza desde la derecha. El header sticky del dashboard tiene z-50 — el drawer y su overlay usan z-40 para no taparlo.

```tsx
import { X } from 'lucide-react'
import { cn } from '@/lib/utils'
import { useClients } from '@/hooks/useClients'
import type { BffClient } from '@/types/api'

interface ClientsDrawerProps {
  estado: 'Activo' | 'Suspendido' | null  // null = cerrado
  onClose: () => void
}

function formatCOP(amount: number): string {
  return new Intl.NumberFormat('es-CO', {
    style: 'currency',
    currency: 'COP',
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(amount)
}

export function ClientsDrawer({ estado, onClose }: ClientsDrawerProps) {
  const isOpen = estado !== null
  const { data, isLoading, isError, error } = useClients()

  const filteredClients: BffClient[] = isOpen && data
    ? data.clients.filter((c) => c.estado === estado)
    : []

  const title = estado === 'Activo'
    ? `Clientes activos (${filteredClients.length})`
    : estado === 'Suspendido'
    ? `Clientes suspendidos (${filteredClients.length})`
    : ''

  return (
    <>
      {/* Overlay — z-40 (debajo del header z-50) */}
      <div
        className={cn(
          'fixed inset-0 z-40 bg-black/50 transition-opacity duration-300',
          isOpen ? 'opacity-100 pointer-events-auto' : 'opacity-0 pointer-events-none'
        )}
        onClick={onClose}
        aria-hidden="true"
      />

      {/* Drawer panel — z-40, 400px ancho, desliza desde derecha */}
      <div
        className={cn(
          'fixed top-0 right-0 z-40 h-full w-[400px] max-w-[95vw]',
          'bg-surface border-l border-border',
          'flex flex-col',
          'transform transition-transform duration-300 ease-in-out',
          isOpen ? 'translate-x-0' : 'translate-x-full'
        )}
        role="dialog"
        aria-modal="true"
        aria-label={title}
      >
        {/* Drawer header */}
        <div className="flex items-center justify-between px-5 py-4 border-b border-border flex-shrink-0">
          <h2 className="text-sm font-semibold text-text-primary">{title}</h2>
          <button
            onClick={onClose}
            className="rounded-lg p-1.5 text-text-muted hover:text-text-primary hover:bg-white/10 transition-colors"
            aria-label="Cerrar panel"
          >
            <X className="h-4 w-4" />
          </button>
        </div>

        {/* Drawer body — scrollable */}
        <div className="flex-1 overflow-y-auto">
          {isLoading && (
            <div className="p-5 space-y-3">
              {Array.from({ length: 6 }).map((_, i) => (
                <div key={i} className="h-10 rounded-lg bg-white/5 animate-pulse" />
              ))}
            </div>
          )}

          {isError && (
            <div className="p-5">
              <p className="text-sm text-error">Error al cargar clientes</p>
              <p className="mt-1 text-xs text-text-muted">
                {error?.message ?? 'No se pudo obtener la lista de clientes'}
              </p>
            </div>
          )}

          {!isLoading && !isError && filteredClients.length === 0 && (
            <div className="p-5">
              <p className="text-sm text-text-muted">No hay clientes en este estado.</p>
            </div>
          )}

          {!isLoading && !isError && filteredClients.length > 0 && (
            <table className="w-full text-sm">
              <thead className="sticky top-0 bg-surface border-b border-border">
                <tr>
                  <th className="px-5 py-3 text-left text-xs font-medium text-text-muted uppercase tracking-wider">
                    Cliente
                  </th>
                  <th className="px-3 py-3 text-left text-xs font-medium text-text-muted uppercase tracking-wider">
                    Plan
                  </th>
                  <th className="px-3 py-3 text-left text-xs font-medium text-text-muted uppercase tracking-wider">
                    Zona
                  </th>
                  <th className="px-5 py-3 text-right text-xs font-medium text-text-muted uppercase tracking-wider">
                    Saldo
                  </th>
                </tr>
              </thead>
              <tbody className="divide-y divide-border">
                {filteredClients.map((client, idx) => (
                  <tr key={idx} className="hover:bg-white/5 transition-colors">
                    <td className="px-5 py-3 text-text-primary font-medium truncate max-w-[140px]">
                      {client.nombre}
                    </td>
                    <td className="px-3 py-3 text-text-secondary truncate max-w-[100px]">
                      {client.plan ?? <span className="text-text-muted italic">—</span>}
                    </td>
                    <td className="px-3 py-3 text-text-secondary truncate max-w-[80px]">
                      {client.zona ?? <span className="text-text-muted italic">—</span>}
                    </td>
                    <td className="px-5 py-3 text-right text-text-primary tabular-nums">
                      {formatCOP(client.saldo)}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
        </div>
      </div>
    </>
  )
}
```

**2b. Modificar client/src/components/metrics/MetricCard.tsx**

Agregar prop opcional `onClick?: () => void`. Cuando existe, el div raíz:
- Cambia a `role="button"` con `tabIndex={0}`
- Agrega `cursor-pointer` y `hover:border-border/80` (feedback visual sutil)
- Maneja `onKeyDown` para accesibilidad (Enter/Space dispara onClick)

```tsx
import { cn } from '@/lib/utils'
import type { LucideIcon } from 'lucide-react'

interface MetricCardProps {
  title: string
  value: string
  subtitle?: string
  icon: LucideIcon
  iconClassName?: string
  className?: string
  onClick?: () => void
}

export function MetricCard({
  title,
  value,
  subtitle,
  icon: Icon,
  iconClassName,
  className,
  onClick,
}: MetricCardProps) {
  const isClickable = onClick !== undefined

  return (
    <div
      className={cn(
        'rounded-xl border border-border bg-surface p-6',
        'transition-colors',
        isClickable && 'cursor-pointer hover:border-zinc-600 select-none',
        className
      )}
      onClick={onClick}
      role={isClickable ? 'button' : undefined}
      tabIndex={isClickable ? 0 : undefined}
      onKeyDown={isClickable ? (e) => { if (e.key === 'Enter' || e.key === ' ') onClick() } : undefined}
    >
      <div className="flex items-start justify-between">
        <div className="flex-1 min-w-0">
          <p className="text-sm font-medium text-text-secondary truncate">{title}</p>
          <p className="mt-2 text-3xl font-bold text-text-primary leading-none">
            {value}
          </p>
          {subtitle && (
            <p className="mt-1 text-xs text-text-muted">{subtitle}</p>
          )}
        </div>
        <div
          className={cn(
            'flex-shrink-0 rounded-lg p-3',
            iconClassName
          )}
        >
          <Icon className="h-6 w-6" aria-hidden="true" />
        </div>
      </div>
    </div>
  )
}
```

**2c. Modificar client/src/components/metrics/MetricsGrid.tsx**

Agregar `useState` para `drawerEstado`. Pasar `onClick` a las dos tarjetas de clientes. Renderizar `ClientsDrawer` al final del JSX (fuera del grid, al mismo nivel).

```tsx
import { useState } from 'react'
import { Users, UserX, DollarSign, TrendingUp } from 'lucide-react'
import { useMetrics } from '@/hooks/useMetrics'
import { MetricCard } from './MetricCard'
import { MetricCardSkeleton } from './MetricCardSkeleton'
import { ClientsDrawer } from './ClientsDrawer'
import type { MetricsData } from '@/types/api'

function formatCOP(amount: number): string {
  return new Intl.NumberFormat('es-CO', {
    style: 'currency',
    currency: 'COP',
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(amount)
}

function formatNumber(n: number): string {
  return new Intl.NumberFormat('es-CO').format(n)
}

interface MetricsGridProps {
  _data?: MetricsData
}

export function MetricsGrid({ _data }: MetricsGridProps = {}) {
  const { data, isLoading, isError, error } = useMetrics()
  const [drawerEstado, setDrawerEstado] = useState<'Activo' | 'Suspendido' | null>(null)

  if (isLoading) {
    return (
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
        <MetricCardSkeleton />
        <MetricCardSkeleton />
        <MetricCardSkeleton />
        <MetricCardSkeleton />
      </div>
    )
  }

  if (isError) {
    return (
      <div className="rounded-xl border border-error/30 bg-error/10 px-6 py-4">
        <p className="text-sm font-medium text-error">
          Error al cargar métricas
        </p>
        <p className="mt-1 text-xs text-text-muted">
          {error?.message ?? 'No se pudieron obtener los datos de Wisphub'}
        </p>
      </div>
    )
  }

  const metrics = _data ?? data

  if (!metrics) return null

  return (
    <>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
        <MetricCard
          title="Clientes activos"
          value={formatNumber(metrics.activeClients)}
          subtitle="Estado Activo en Wisphub"
          icon={Users}
          iconClassName="text-text-secondary"
          onClick={() => setDrawerEstado('Activo')}
        />
        <MetricCard
          title="Clientes suspendidos"
          value={formatNumber(metrics.suspendedClients)}
          subtitle="Estado Suspendido en Wisphub"
          icon={UserX}
          iconClassName="text-error"
          onClick={() => setDrawerEstado('Suspendido')}
        />
        <MetricCard
          title="Deuda pendiente"
          value={formatCOP(metrics.pendingDebt)}
          subtitle="Saldo positivo total"
          icon={DollarSign}
          iconClassName="text-brand"
        />
        <MetricCard
          title="Ingresos del mes"
          value={formatCOP(metrics.monthlyRevenue)}
          subtitle="Facturas pagadas este mes"
          icon={TrendingUp}
          iconClassName="text-brand"
        />
      </div>

      <ClientsDrawer
        estado={drawerEstado}
        onClose={() => setDrawerEstado(null)}
      />
    </>
  )
}
```

NOTA: Las tarjetas "Deuda pendiente" e "Ingresos del mes" NO reciben `onClick` — permanecen como cards no clickeables. App.tsx no requiere cambios.
  </action>
  <verify>
    En el navegador: (1) Click en "Clientes activos" abre drawer desde derecha con título "Clientes activos (N)" y lista de clientes Activo. (2) Click en "Clientes suspendidos" abre drawer con clientes Suspendido. (3) Click en overlay o botón X cierra el drawer. (4) El header BEEPYRED permanece visible y clickeable encima del overlay. (5) Las tarjetas de deuda e ingresos no tienen cursor pointer. TypeScript: cd client && npx tsc --noEmit pasa sin errores.
  </verify>
  <done>
    ClientsDrawer renderiza lista filtrada por estado con columnas Cliente/Plan/Zona/Saldo. Saldo formateado en COP con locale es-CO. MetricCard acepta onClick opcional. MetricsGrid maneja drawerEstado con useState. El header con z-50 nunca queda tapado (overlay y drawer usan z-40). tsc --noEmit sin errores.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Drawer de clientes completo: BFF endpoint /api/clients, hook useClients, componente ClientsDrawer con overlay y animación de slide-in, MetricCard clickeable, MetricsGrid con estado de drawer.
  </what-built>
  <how-to-verify>
    1. Iniciar servidor: cd server && npm run dev (debe mostrar "[BFF] Server running on http://localhost:3001")
    2. Iniciar cliente: cd client && npm run dev (debe abrir http://localhost:5173)
    3. Verificar endpoint: curl http://localhost:3001/api/clients — debe devolver { success: true, data: { clients: [...], totalCount: N } } con N > 0
    4. En el dashboard, hacer hover sobre "Clientes activos" — el cursor debe cambiar a pointer
    5. Click en "Clientes activos" — debe deslizarse panel desde la derecha con título "Clientes activos (N)"
    6. Verificar que la tabla muestra columnas: Cliente, Plan, Zona, Saldo
    7. Verificar que Saldo tiene formato COP (ej: "$ 0" o "$ 150.000")
    8. Verificar que el header "BEEPYRED Dashboard" permanece visible y no queda tapado por el overlay
    9. Click fuera del drawer (en el overlay oscuro) — debe cerrarse
    10. Click en "Clientes suspendidos" — debe abrir con título "Clientes suspendidos (N)" y solo clientes suspendidos
    11. Click en el botón X — debe cerrar el drawer
    12. Las tarjetas "Deuda pendiente" e "Ingresos del mes" no deben tener cursor pointer ni abrir drawer
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si todo funciona, o describe cualquier problema encontrado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → BFF | El admin hace click, el frontend llama /api/clients |
| BFF → Wisphub | El BFF llama a la API de Wisphub con el token secreto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ve0-01 | Information Disclosure | GET /api/clients | accept | Endpoint interno sin auth — admin único por diseño (D-00); el token Wisphub nunca llega al browser (BFF proxy) |
| T-ve0-02 | Denial of Service | GET /api/clients → Wisphub | mitigate | Cache 30s implementado en clients.ts — misma estrategia que /api/metrics; limita requests a Wisphub a 1 cada 30s por endpoint |
| T-ve0-03 | Spoofing | Datos de clientes en response | accept | Wisphub es la fuente de verdad; validación con Zod WisphubClientSchema descarta registros malformados con warning en log |
</threat_model>

<verification>
- curl http://localhost:3001/api/clients devuelve 200 con lista de clientes normalizados
- Segunda llamada inmediata devuelve cached: true
- cd client && npx tsc --noEmit — 0 errores
- Drawer se abre y cierra sin romper el layout del dashboard
- Header con z-50 siempre visible sobre el overlay z-40
</verification>

<success_criteria>
- GET /api/clients retorna clientes con campos: nombre, estado, saldo (number), plan (string|null), zona (string|null)
- Cache de 30s funciona (segunda request devuelve cached: true)
- Click en "Clientes activos" abre drawer con clientes estado=Activo
- Click en "Clientes suspendidos" abre drawer con clientes estado=Suspendido
- Drawer se cierra con X o click en overlay
- Header del dashboard nunca queda tapado (z-50 > z-40)
- Saldo formateado con Intl.NumberFormat locale es-CO
- TypeScript compila sin errores
</success_criteria>

<output>
After completion, create `.planning/quick/260407-ve0-drawer-panel-lateral-de-clientes-activos/260407-ve0-SUMMARY.md` following the summary template.
</output>
