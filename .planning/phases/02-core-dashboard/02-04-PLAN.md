---
phase: 02-core-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - client/src/components/invoices/InvoiceList.tsx
  - client/src/components/invoices/InvoiceListSkeleton.tsx
  - client/src/components/invoices/InvoiceRow.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - FAC-01
  - FAC-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "El admin ve nombre del cliente, monto owed, y días de atraso por cada factura pendiente"
    - "Los días de atraso se calculan desde fecha_vencimiento hasta hoy"
    - "Facturas con saldo <= 0 no aparecen en la lista (filtro primario: saldo > 0 per FAC-01)"
    - "El monto muestra formato colombiano: $ 9.551.300"
    - "La lista muestra skeleton de filas mientras carga"
    - "Si useInvoices devuelve isError, la sección muestra error inline"
    - "Si no hay facturas pendientes, muestra mensaje vacío claro"
  artifacts:
    - path: "client/src/components/invoices/InvoiceList.tsx"
      provides: "Contenedor conectado a useInvoices con estados loading/error/empty"
      contains: "useInvoices"
    - path: "client/src/components/invoices/InvoiceRow.tsx"
      provides: "Fila individual de factura con días de atraso calculados"
      contains: "fecha_vencimiento"
    - path: "client/src/components/invoices/InvoiceListSkeleton.tsx"
      provides: "Skeleton de filas para estado de carga"
      contains: "animate-pulse"
  key_links:
    - from: "client/src/components/invoices/InvoiceList.tsx"
      to: "client/src/hooks/useInvoices.ts"
      via: "useInvoices import"
      pattern: "useInvoices"
    - from: "client/src/components/invoices/InvoiceRow.tsx"
      to: "BffInvoice.fecha_vencimiento"
      via: "cálculo días de atraso"
      pattern: "fecha_vencimiento"
---

<objective>
Construir el listado de facturas pendientes con cálculo de días de atraso, estados de carga y error, y formato de moneda colombiana. Este plan corre después del Plan 03 (wave 4) para evitar conflicto de escritura en App.tsx.

Purpose: El admin necesita ver qué clientes tienen facturas vencidas y cuántos días llevan sin pagar. El filtro primario es saldo > 0 (FAC-01) — no por campo estado.
Output: InvoiceList, InvoiceRow, InvoiceListSkeleton, y App.tsx con la sección de facturas reemplazando el placeholder del Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-dashboard/02-02-SUMMARY.md
@client/src/types/api.ts
@client/src/hooks/useInvoices.ts
@client/tailwind.config.ts

<interfaces>
<!-- Contratos de Plan 02 que este plan consume -->

useInvoices() retorna:
```typescript
{
  data: InvoicesData | undefined,
  isLoading: boolean,
  isError: boolean,
  error: Error | null,
  dataUpdatedAt: number,
  refetch: () => void,
}
```

InvoicesData:
```typescript
{
  invoices: BffInvoice[],
  totalCount: number,
  fetchedAt: string,
}
```

BffInvoice (campos relevantes para esta UI):
```typescript
{
  id_factura: number,
  fecha_vencimiento: string,    // ISO date "2025-01-15" — calcular días de atraso desde hoy
  fecha_pago: string | null,
  estado: 'Pagada' | 'Pendiente' | 'Vencida' | 'Cancelada' | string,
  total: number,                // monto total en pesos colombianos
  saldo: number,                // monto pendiente (0 si pagada) — FILTRO PRIMARIO FAC-01
  total_cobrado: number,
  cliente: { nombre: string },
}
```

Criterio de filtrado (FAC-01): invoice.saldo > 0 — este es el criterio primario.
No filtrar solo por invoice.estado — el saldo es la fuente de verdad para deuda pendiente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear InvoiceRow + InvoiceListSkeleton con lógica de días de atraso</name>
  <files>
    client/src/components/invoices/InvoiceRow.tsx,
    client/src/components/invoices/InvoiceListSkeleton.tsx
  </files>

  <read_first>
    - client/src/types/api.ts — BffInvoice interface (fecha_vencimiento, saldo, cliente.nombre, estado)
    - client/tailwind.config.ts — tokens: border, surface, text-*, error (D-06)
    - client/src/lib/utils.ts — cn() para combinar clases
  </read_first>

  <action>
Crear `client/src/components/invoices/InvoiceRow.tsx`:

```tsx
import { cn } from '@/lib/utils'
import type { BffInvoice } from '@/types/api'

interface InvoiceRowProps {
  invoice: BffInvoice
}

// ── Formateo de pesos colombianos ──
function formatCOP(amount: number): string {
  return new Intl.NumberFormat('es-CO', {
    style: 'currency',
    currency: 'COP',
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(amount)
}

// ── Calcular días de atraso ──
// Recibe fecha_vencimiento como ISO date string ("2025-01-15")
// Devuelve número de días entre fecha_vencimiento y hoy
// Si fecha_vencimiento es futura, devuelve 0 (no está vencida aún)
export function calcularDiasAtraso(fechaVencimiento: string): number {
  const hoy = new Date()
  hoy.setHours(0, 0, 0, 0)                          // normalizar a medianoche local

  // Parsear como fecha local para evitar off-by-one por timezone
  // "2025-01-15" → [2025, 1, 15] → new Date(2025, 0, 15)
  const partes = fechaVencimiento.split('-').map(Number)
  const vencimiento = new Date(partes[0], partes[1] - 1, partes[2])

  if (vencimiento >= hoy) return 0                   // no vencida aún

  const diffMs = hoy.getTime() - vencimiento.getTime()
  return Math.floor(diffMs / (1000 * 60 * 60 * 24))
}

export function InvoiceRow({ invoice }: InvoiceRowProps) {
  const diasAtraso = calcularDiasAtraso(invoice.fecha_vencimiento)
  // Usar saldo si > 0, sino total (saldo es la fuente de verdad de deuda — FAC-01)
  const montoMostrar = invoice.saldo > 0 ? invoice.saldo : invoice.total

  // Color de badge según días de atraso
  const badgeClassName = cn(
    'inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium',
    diasAtraso === 0 && 'bg-border text-text-secondary',
    diasAtraso > 0 && diasAtraso <= 30 && 'bg-error/20 text-error',
    diasAtraso > 30 && diasAtraso <= 60 && 'bg-error/30 text-error',
    diasAtraso > 60 && 'bg-error/40 text-error font-bold',
  )

  return (
    <tr className="border-b border-border last:border-0 hover:bg-border/20 transition-colors">
      {/* Nombre del cliente */}
      <td className="px-4 py-3 text-sm text-text-primary">
        {invoice.cliente.nombre}
      </td>

      {/* ID factura */}
      <td className="px-4 py-3 text-sm text-text-muted font-mono">
        #{invoice.id_factura}
      </td>

      {/* Monto owed */}
      <td className="px-4 py-3 text-sm text-text-primary font-medium text-right">
        {formatCOP(montoMostrar)}
      </td>

      {/* Días de atraso */}
      <td className="px-4 py-3 text-right">
        <span className={badgeClassName}>
          {diasAtraso === 0
            ? 'Pendiente'
            : `${diasAtraso} día${diasAtraso !== 1 ? 's' : ''}`}
        </span>
      </td>
    </tr>
  )
}
```

Crear `client/src/components/invoices/InvoiceListSkeleton.tsx`:

```tsx
// Skeleton rows — 5 filas de placeholder
export function InvoiceListSkeleton() {
  return (
    <div className="rounded-xl border border-border bg-surface overflow-hidden">
      {/* Header tabla */}
      <div className="border-b border-border px-4 py-3 bg-border/20">
        <div className="h-4 w-48 rounded bg-border" />
      </div>

      {/* Filas skeleton */}
      <div className="divide-y divide-border animate-pulse">
        {Array.from({ length: 5 }).map((_, i) => (
          <div key={i} className="flex items-center gap-4 px-4 py-3">
            {/* Nombre */}
            <div className="h-4 flex-1 rounded bg-border" />
            {/* ID */}
            <div className="h-4 w-16 rounded bg-border" />
            {/* Monto */}
            <div className="h-4 w-24 rounded bg-border" />
            {/* Badge */}
            <div className="h-6 w-20 rounded-full bg-border" />
          </div>
        ))}
      </div>
    </div>
  )
}
```
  </action>

  <verify>
    <automated>
      grep -n "calcularDiasAtraso" client/src/components/invoices/InvoiceRow.tsx
      grep -n "fecha_vencimiento" client/src/components/invoices/InvoiceRow.tsx
      grep -n "animate-pulse" client/src/components/invoices/InvoiceListSkeleton.tsx
      grep -n "formatCOP" client/src/components/invoices/InvoiceRow.tsx
      cd client && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "calcularDiasAtraso" client/src/components/invoices/InvoiceRow.tsx` devuelve la función exportada
    - `grep "fecha_vencimiento" client/src/components/invoices/InvoiceRow.tsx` devuelve uso en el cálculo
    - `grep "es-CO" client/src/components/invoices/InvoiceRow.tsx` devuelve resultado (formato colombiano)
    - `grep "animate-pulse" client/src/components/invoices/InvoiceListSkeleton.tsx` devuelve resultado
    - `grep "export function calcularDiasAtraso" client/src/components/invoices/InvoiceRow.tsx` — exportada (testeable)
    - `cd client && npx tsc --noEmit` sin errores en los archivos nuevos
  </acceptance_criteria>

  <done>InvoiceRow con calcularDiasAtraso exportada, badge de color según días vencida, formatCOP. InvoiceListSkeleton con 5 filas animate-pulse.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear InvoiceList (filtro saldo > 0 per FAC-01) y reemplazar placeholder en App.tsx</name>
  <files>
    client/src/components/invoices/InvoiceList.tsx,
    client/src/App.tsx
  </files>

  <read_first>
    - client/src/hooks/useInvoices.ts — importar useInvoices (Plan 02 Task 2)
    - client/src/types/api.ts — BffInvoice type
    - client/src/components/invoices/InvoiceRow.tsx — Task 1 prerequisito
    - client/src/components/invoices/InvoiceListSkeleton.tsx — Task 1 prerequisito
    - client/src/App.tsx — leer estado actual (post Plan 03) antes de modificar
  </read_first>

  <action>
Crear `client/src/components/invoices/InvoiceList.tsx`:

```tsx
import { useInvoices } from '@/hooks/useInvoices'
import { InvoiceRow } from './InvoiceRow'
import { InvoiceListSkeleton } from './InvoiceListSkeleton'
import type { BffInvoice } from '@/types/api'

// Filtrar facturas con deuda real — criterio primario: saldo > 0 (FAC-01)
// El saldo es la fuente de verdad para deuda pendiente en Wisphub.
// No filtrar solo por invoice.estado porque una factura puede tener estado
// 'Pendiente' pero saldo = 0 (ya cobrada sin actualizar estado).
function isPendingInvoice(invoice: BffInvoice): boolean {
  return invoice.saldo > 0
}

export function InvoiceList() {
  const { data, isLoading, isError, error } = useInvoices()

  if (isLoading) {
    return <InvoiceListSkeleton />
  }

  if (isError) {
    return (
      <div className="rounded-xl border border-error/30 bg-error/10 px-6 py-4">
        <p className="text-sm font-medium text-error">
          Error al cargar facturas
        </p>
        <p className="mt-1 text-xs text-text-muted">
          {error?.message ?? 'No se pudieron obtener las facturas de Wisphub'}
        </p>
      </div>
    )
  }

  if (!data) return null

  const pendingInvoices = data.invoices.filter(isPendingInvoice)

  // Estado vacío — sin facturas con saldo pendiente
  if (pendingInvoices.length === 0) {
    return (
      <div className="rounded-xl border border-border bg-surface px-6 py-12 text-center">
        <p className="text-text-secondary font-medium">
          Sin facturas pendientes
        </p>
        <p className="mt-1 text-sm text-text-muted">
          Todos los clientes están al día
        </p>
      </div>
    )
  }

  return (
    <div className="rounded-xl border border-border bg-surface overflow-hidden">
      <div className="border-b border-border px-4 py-3 bg-border/20 flex items-center justify-between">
        <p className="text-sm font-medium text-text-secondary">
          Facturas pendientes
        </p>
        <span className="text-xs text-text-muted">
          {pendingInvoices.length} factura{pendingInvoices.length !== 1 ? 's' : ''}
        </span>
      </div>

      <div className="overflow-x-auto">
        <table className="w-full min-w-[600px]">
          <thead>
            <tr className="border-b border-border">
              <th className="px-4 py-2 text-left text-xs font-medium uppercase tracking-wider text-text-muted">
                Cliente
              </th>
              <th className="px-4 py-2 text-left text-xs font-medium uppercase tracking-wider text-text-muted">
                Factura
              </th>
              <th className="px-4 py-2 text-right text-xs font-medium uppercase tracking-wider text-text-muted">
                Monto
              </th>
              <th className="px-4 py-2 text-right text-xs font-medium uppercase tracking-wider text-text-muted">
                Atraso
              </th>
            </tr>
          </thead>
          <tbody>
            {pendingInvoices.map((invoice) => (
              <InvoiceRow key={invoice.id_factura} invoice={invoice} />
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

Actualizar `client/src/App.tsx` para reemplazar el placeholder de facturas con InvoiceList real.

**IMPORTANTE:** Este plan corre en wave 4, después de Plan 03 (wave 3). Leer el estado actual de App.tsx antes de modificar para preservar DashboardLayout y MetricsGrid ya integrados.

Reemplazar el bloque del placeholder de facturas:
```tsx
// Antes (placeholder del Plan 03):
<div className="rounded-xl border border-border bg-surface p-6">
  <p className="text-text-muted text-sm">
    Lista de facturas — próximo plan
  </p>
</div>

// Después (componente real):
<InvoiceList />
```

El import a agregar al inicio de App.tsx:
```tsx
import { InvoiceList } from '@/components/invoices/InvoiceList'
```
  </action>

  <verify>
    <automated>
      grep -n "useInvoices" client/src/components/invoices/InvoiceList.tsx
      grep -n "isPendingInvoice" client/src/components/invoices/InvoiceList.tsx
      grep -n "saldo > 0" client/src/components/invoices/InvoiceList.tsx
      grep -n "InvoiceList" client/src/App.tsx
      grep -n "Sin facturas pendientes" client/src/components/invoices/InvoiceList.tsx
      cd client && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "useInvoices" client/src/components/invoices/InvoiceList.tsx` devuelve el import y uso
    - `grep "isPendingInvoice" client/src/components/invoices/InvoiceList.tsx` devuelve la función filtro
    - `grep "saldo > 0" client/src/components/invoices/InvoiceList.tsx` — filtro primario por saldo (FAC-01)
    - `grep "Sin facturas pendientes" client/src/components/invoices/InvoiceList.tsx` — estado vacío presente
    - `grep "isError" client/src/components/invoices/InvoiceList.tsx` devuelve resultado (estado error)
    - `grep "InvoiceList" client/src/App.tsx` devuelve resultado (componente integrado)
    - `grep "MetricsGrid" client/src/App.tsx` devuelve resultado (no pisado por este plan)
    - `cd client && npx tsc --noEmit` sin errores
  </acceptance_criteria>

  <done>InvoiceList conectado a useInvoices con filtro saldo > 0 (FAC-01), estados loading/error/empty, tabla con header. App.tsx actualizado con InvoiceList real reemplazando el placeholder del Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| BffInvoice.cliente.nombre → render | Nombre de cliente se renderiza — potencial XSS si contiene HTML |
| fecha_vencimiento parsing | String de fecha parseada como Date — vulnerable a fechas malformadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | XSS via cliente.nombre | mitigate | React escapa automáticamente strings en JSX. cliente.nombre se renderiza como texto, no HTML. No se usa dangerouslySetInnerHTML. |
| T-02-11 | Tampering | fecha_vencimiento parsing | mitigate | calcularDiasAtraso usa split('-').map(Number) — si la fecha es malformada, partes[0]/[1]/[2] serán NaN y new Date(NaN, NaN, NaN) producirá Invalid Date, resultando en diasAtraso = 0. Aceptable — muestra "Pendiente" sin crashear. |
| T-02-12 | Information Disclosure | InvoiceList error.message | accept | El mensaje de error es para el admin. No expone datos de clientes. |
</threat_model>

<verification>
Verificación global del plan:

1. `grep "saldo > 0" client/src/components/invoices/InvoiceList.tsx` — filtro primario FAC-01 confirmado
2. `grep "calcularDiasAtraso" client/src/components/invoices/InvoiceRow.tsx` — lógica exportada
3. `cd client && npx tsc --noEmit` — TypeScript limpio
4. Con servidor BFF corriendo: http://localhost:5173 muestra tabla de facturas (o mensaje vacío si no hay pendientes)
</verification>

<success_criteria>
- InvoiceRow calcula días de atraso desde fecha_vencimiento hasta hoy (parseando como fecha local)
- Días de atraso > 60 tiene badge rojo intenso; 31-60 rojo; 0-30 rojo suave; 0 días = "Pendiente"
- Monto usa formatCOP con locale es-CO
- InvoiceList filtra por saldo > 0 (FAC-01) — nunca muestra facturas con saldo = 0
- Estado vacío muestra "Sin facturas pendientes — Todos los clientes están al día"
- Estado error muestra mensaje inline con border-error/30
- App.tsx integra InvoiceList en la sección de facturas, preservando MetricsGrid
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-core-dashboard/02-04-SUMMARY.md` con el formato estándar de summary.
</output>
