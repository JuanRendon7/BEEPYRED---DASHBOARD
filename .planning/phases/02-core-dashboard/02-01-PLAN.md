---
phase: 02-core-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/postcss.config.js
  - client/tailwind.config.ts
  - client/src/index.css
  - client/components.json
  - client/src/lib/utils.ts
autonomous: true
requirements:
  - UX-01
  - UX-03

must_haves:
  truths:
    - "Tailwind v3 (3.4.19) genera clases CSS correctamente en el browser"
    - "El token de color brand.DEFAULT existe en tailwind.config.ts como #F5A800 (per D-01)"
    - "La clase `dark` en <html> activa dark mode en todos los componentes"
    - "shadcn/ui init produce components.json con baseColor zinc y src/lib/utils.ts"
    - "El componente Button de shadcn/ui se puede importar desde @/components/ui/button"
  artifacts:
    - path: "client/tailwind.config.ts"
      provides: "Configuración Tailwind v3 con tokens de color BEEPYRED (D-01 a D-07)"
      contains: "brand: { DEFAULT: '#F5A800' }"
    - path: "client/postcss.config.js"
      provides: "PostCSS con tailwindcss y autoprefixer"
    - path: "client/components.json"
      provides: "Configuración shadcn/ui con baseColor zinc"
      contains: "\"baseColor\""
    - path: "client/src/lib/utils.ts"
      provides: "Función cn() para combinar clases Tailwind"
      contains: "clsx"
    - path: "client/src/components/ui/button.tsx"
      provides: "Componente Button shadcn/ui base"
  key_links:
    - from: "client/src/index.css"
      to: "tailwind.config.ts"
      via: "@tailwind directives"
      pattern: "@tailwind base"
    - from: "client/index.html"
      to: "dark mode"
      via: "class=\"dark\" en <html>"
      pattern: "class=\"dark\""
---

<objective>
Configurar el design system completo: Tailwind CSS v3.4.19 + PostCSS + tailwind.config.ts con todos los tokens de color BEEPYRED (D-01 a D-07) + shadcn/ui inicializado con dark mode y base color zinc. Este plan es el bloqueo para todos los demás — sin él, ningún componente puede usar clases Tailwind ni shadcn/ui.

Purpose: Establecer la base visual del dashboard. El dark mode con `class` strategy y los tokens de color brand deben funcionar antes de escribir cualquier componente.
Output: Tailwind v3 activo, tailwind.config.ts con tokens BEEPYRED exactos según D-01 a D-07, shadcn/ui inicializado con zinc, componente Button instalado como prueba de humo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@client/package.json
@client/vite.config.ts
@client/src/main.tsx
@client/index.html
@client/src/index.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Instalar Tailwind v3 + PostCSS y crear tailwind.config.ts con tokens BEEPYRED (D-01 a D-07)</name>
  <files>
    client/package.json,
    client/postcss.config.js,
    client/tailwind.config.ts,
    client/src/index.css,
    client/index.html
  </files>

  <read_first>
    - client/package.json — para verificar que tailwindcss NO está ya instalado
    - client/src/index.css — tiene @tailwind directives placeholder; confirmar que están presentes
    - client/index.html — debe recibir class="dark" en el elemento html
  </read_first>

  <action>
**CRITICAL:** Instalar `tailwindcss@3.4.19` exacto — NO usar `tailwindcss@latest` (v4 es incompatible con shadcn/ui).

Desde el directorio `client/`:

```bash
cd client && npm install -D tailwindcss@3.4.19 postcss@8.4.47 autoprefixer@10.4.20
```

Crear `client/postcss.config.js` (formato CJS — Tailwind v3 lo requiere):
```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

Crear `client/tailwind.config.ts` con tokens exactos de D-01 a D-07:
```ts
import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: 'class',
  content: [
    './index.html',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        // D-01: Primary brand — gold amber extraído del logo BEEPYRED
        brand: {
          DEFAULT: '#F5A800',
          hover: '#D98F00',
        },
        // D-02: Fondo de página — near-black
        page: '#0A0A0A',
        // D-03: Superficie (cards, paneles) — zinc-900
        surface: '#18181B',
        // D-04: Borde — zinc-800
        border: '#27272A',
        // D-05: Texto
        text: {
          primary: '#FFFFFF',
          secondary: '#A1A1AA',   // zinc-400
          muted: '#71717A',       // zinc-500 — para subtítulos y labels
        },
        // D-06: Error accent — red-500
        error: '#EF4444',
      },
    },
  },
  plugins: [],
}

export default config
```

Actualizar `client/src/index.css` (reemplazar contenido completo):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  * {
    box-sizing: border-box;
  }
  body {
    @apply bg-page text-text-primary;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    -webkit-font-smoothing: antialiased;
  }
}
```

Actualizar `client/index.html` — agregar `class="dark"` al elemento `<html>`:
```html
<html lang="es" class="dark">
```
  </action>

  <verify>
    <automated>
      cd client && npx tsc --noEmit 2>&1 | head -20
      # Verificar tokens correctos según D-01 a D-07
      grep -n "brand" client/tailwind.config.ts
      grep -n "F5A800" client/tailwind.config.ts
      grep -n "0A0A0A" client/tailwind.config.ts
      grep -n "18181B" client/tailwind.config.ts
      grep -n "27272A" client/tailwind.config.ts
      grep -n "A1A1AA" client/tailwind.config.ts
      grep -n "EF4444" client/tailwind.config.ts
      grep -n "darkMode.*class" client/tailwind.config.ts
      grep -n 'class="dark"' client/index.html
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "tailwindcss" client/package.json` devuelve `"tailwindcss": "3.4.19"` (exacto, sin ^)
    - `grep "darkMode" client/tailwind.config.ts` devuelve `darkMode: 'class'`
    - `grep "F5A800" client/tailwind.config.ts` devuelve la definición de brand.DEFAULT (D-01)
    - `grep "0A0A0A" client/tailwind.config.ts` devuelve page: '#0A0A0A' (D-02)
    - `grep "18181B" client/tailwind.config.ts` devuelve surface: '#18181B' (D-03)
    - `grep "27272A" client/tailwind.config.ts` devuelve border: '#27272A' (D-04)
    - `grep "A1A1AA" client/tailwind.config.ts` devuelve text.secondary: '#A1A1AA' (D-05)
    - `grep "EF4444" client/tailwind.config.ts` devuelve error: '#EF4444' (D-06)
    - `grep "class=\"dark\"" client/index.html` devuelve resultado
    - `client/postcss.config.js` existe y contiene `tailwindcss: {}`
    - `grep "@tailwind base" client/src/index.css` devuelve resultado
    - NO existen tokens surface-2, success, warning, info, danger en tailwind.config.ts
  </acceptance_criteria>

  <done>Tailwind v3.4.19 instalado, postcss.config.js creado, tailwind.config.ts con tokens exactos D-01 a D-07 (brand #F5A800, page #0A0A0A, surface #18181B, border #27272A, text.secondary #A1A1AA, error #EF4444), class="dark" en index.html, index.css con @tailwind directives y @layer base.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inicializar shadcn/ui con base-color zinc y agregar componentes Button + Badge como prueba de humo</name>
  <files>
    client/components.json,
    client/src/lib/utils.ts,
    client/src/components/ui/button.tsx,
    client/src/components/ui/badge.tsx,
    client/package.json
  </files>

  <read_first>
    - client/tailwind.config.ts — debe existir (Task 1 prerequisito)
    - client/vite.config.ts — tiene alias @/* -> ./src/* (necesario para shadcn paths)
    - client/tsconfig.app.json — debe tener paths @/* para que shadcn/ui resuelva imports
  </read_first>

  <action>
**IMPORTANTE:** shadcn/ui usa `npx shadcn@latest init` con `--base-color zinc` para alinear con los tokens zinc de D-03 a D-05. No usar `shadcn-ui` (paquete antiguo) — usar `shadcn`.

Desde el directorio `client/`:

```bash
cd client && npx shadcn@latest init --yes --base-color zinc --css-variables
```

Si el comando interactivo no puede completarse con `--yes`, usar los valores:
- Style: Default
- Base color: **Zinc** (NO slate — per D-03/D-04/D-05 zinc tokens)
- CSS variables: Yes

El comando crea:
- `client/components.json` — configuración shadcn
- `client/src/lib/utils.ts` — función `cn()` con clsx + tailwind-merge

**VERIFICAR** que `components.json` tenga baseColor zinc:
```json
{
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/index.css",
    "baseColor": "zinc",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils"
  }
}
```

Si `components.json` se generó con `"baseColor": "slate"`, editar manualmente para cambiar a `"baseColor": "zinc"`.

**NOTA:** shadcn/ui init puede modificar `tailwind.config.ts` para agregar CSS variables (--background, --foreground, etc.) y agregar CSS custom properties en `index.css`. Esto es ESPERADO y DESEABLE. Los tokens de color que definimos en Task 1 se mantienen en `theme.extend.colors` — shadcn agrega sus propias variables en la capa base.

Instalar componentes base necesarios:
```bash
cd client && npx shadcn@latest add button badge
```

Instalar dependencias que shadcn requiere si no están instaladas automáticamente:
```bash
cd client && npm install clsx@2.1.1 tailwind-merge@2.5.4 class-variance-authority@0.7.1
```
  </action>

  <verify>
    <automated>
      grep -n "cn" client/src/lib/utils.ts
      grep -n "clsx" client/src/lib/utils.ts
      grep -n "zinc" client/components.json
      ls client/src/components/ui/
    </automated>
  </verify>

  <acceptance_criteria>
    - `client/components.json` existe y `grep "zinc" client/components.json` devuelve `"baseColor": "zinc"` (NO slate)
    - `client/src/lib/utils.ts` existe y `grep "clsx" client/src/lib/utils.ts` devuelve resultado
    - `client/src/components/ui/button.tsx` existe
    - `client/src/components/ui/badge.tsx` existe
    - `grep "clsx" client/package.json` devuelve resultado (clsx instalado)
    - `grep "tailwind-merge" client/package.json` devuelve resultado
    - `cd client && npx tsc --noEmit` completa sin errores relacionados a @/components o @/lib
  </acceptance_criteria>

  <done>shadcn/ui inicializado con base-color zinc (per D-03/D-04/D-05), components.json creado, src/lib/utils.ts con cn(), componentes Button y Badge instalados en src/components/ui/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| npm registry → client/node_modules | Paquetes de terceros (tailwindcss, shadcn) instalados en dev |
| tailwind.config.ts → CSS generado | Tailwind genera CSS en build time — no data runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tailwind.config.ts tokens | accept | Config vive en git; cualquier cambio es visible en diff. No data sensitiva. |
| T-02-02 | Information Disclosure | CSS generado | accept | El CSS generado no contiene datos sensibles — solo tokens de color y utilidades |
| T-02-03 | Tampering | shadcn/ui componentes | accept | Los componentes se copian al repo (owned code) — se pueden auditar. No se inyecta código remoto en runtime. |
</threat_model>

<verification>
Verificación global del plan:

1. `cd client && npm run build` completa sin errores (Tailwind v3 + shadcn/ui configurados correctamente)
2. `cd client && npm run dev` inicia sin errores en consola relacionados a PostCSS o Tailwind
3. El browser muestra fondo near-black (#0A0A0A) en la página raíz — confirma que class="dark" + bg-page funciona
4. `grep "tailwindcss.*3.4.19" client/package.json` — versión exacta
5. `grep "zinc" client/components.json` — confirma baseColor zinc, NO slate
</verification>

<success_criteria>
- Tailwind v3.4.19 instalado y generando CSS correctamente
- tailwind.config.ts tiene exactamente los tokens D-01 a D-07: brand.DEFAULT #F5A800, page #0A0A0A, surface #18181B, border #27272A, text.primary #FFFFFF, text.secondary #A1A1AA, error #EF4444
- NO existen tokens surface-2, success, warning, info, danger (no forman parte de D-01 a D-07)
- shadcn/ui inicializado con baseColor zinc (alineado con zinc tokens D-03/D-04/D-05)
- components.json apunta a tailwind.config.ts con baseColor zinc
- cn() disponible en @/lib/utils
- Button y Badge instalados como prueba de humo
- class="dark" en index.html activa dark mode
- npm run build pasa sin errores
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-core-dashboard/02-01-SUMMARY.md` con el formato estándar de summary.
</output>
